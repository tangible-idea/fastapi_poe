# Core dependencies
pydantic==1.10.9  # Maintained for instagrapi compatibility
fastapi>=0.103.0,<0.104.0  # Compatible with pydantic 1.x
instagrapi==2.0.0
httpx>=0.26.0,<0.28.0  # For compatibility with multiple packages
httpx_sse==0.3.1
pytest==7.4.3
Requests==2.31.0
sse_starlette==1.6.5
tinydb==4.8.0
typing_extensions==4.8.0  # Required version for compatibility
uvicorn==0.24.0.post1
Pillow==10.0.1
python-multipart==0.0.6
openai
#sse-starlette==2.3.5

# Install fastapi-poe separately if needed
# pip install fastapi-poe --no-dependencies